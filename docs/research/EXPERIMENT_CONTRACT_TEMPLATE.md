# Experiment Contract Template

## Experiment Name

`UNVERIFIED`

## Date

`UNVERIFIED`

## Baseline Commit

`UNVERIFIED`

## Baseline Configuration

`UNVERIFIED`

## Problem

`UNVERIFIED`

## Evidence

Log paths and exact evidence:

`UNVERIFIED`

## Gate Decision

PASS or FAIL from `sdcl-research-gate`:

`UNVERIFIED`

## Gate Output Path

`UNVERIFIED`

## Hypothesis

Must include the word `falsifiable` or a direct falsification statement.

`UNVERIFIED`

## Mechanism

`UNVERIFIED`

## Changed Files

`UNVERIFIED`

## Independent Variable

State `one primary variable:` and name it.

`UNVERIFIED`

## Fixed Variables

Dataset, protocol, seed, cmlabel, epochs, checkpoint source, evaluation:

`UNVERIFIED`

## Intermediate Metric

Name the cluster, outlier, coverage, confidence, loss, mAP, or mINP diagnostic expected to move first.

`UNVERIFIED`

## Falsification Result

State the result that would make the decision `reject`.

`UNVERIFIED`

## Main Metrics

- all-search Rank-1/mAP/mINP.
- indoor Rank-1/mAP/mINP.

## Diagnostic Metrics

Cluster counts, outliers, pseudo-label coverage, cross-modal confidence, loss range, checkpoint trajectory:

`UNVERIFIED`

## Dataset And Protocol

`UNVERIFIED`

## Seeds

Diagnostic seed:

Full seed list:

Repeated-baseline comparison source:

`UNVERIFIED`

## Baseline Variance

Baseline variance source, standard deviation/range, or explicit diagnostic-only exception:

`UNVERIFIED`

## Budget

`UNVERIFIED`

## Training Command

`UNVERIFIED`

## Monitor Command

`UNVERIFIED`

## Monitor Log Path

`UNVERIFIED`

## Log Directory

`UNVERIFIED`

## Summary Path

`UNVERIFIED`

## Parser Command

Must call `scripts/parse_sysu_log.py`.

`UNVERIFIED`

## Registry Row

Experiment ID and expected row status in `experiments/registry.csv`.

`UNVERIFIED`

## Checkpoint Plan

Paths for `checkpoint.pth.tar`, `model_best.pth.tar`, `20model_best.pth.tar`, checkpoint source, and whether full-state restore is used.

`UNVERIFIED`

## Environment

Python, CUDA, PyTorch, GPU, command environment, and Git commit or `UNVERIFIED` exception.

`UNVERIFIED`

## Determinism

Python/NumPy/Torch/CUDA seeds, worker seed policy, sampler seed, cudnn, TF32, deterministic algorithm policy.

`UNVERIFIED`

## Smoke Test

Commands and expected pass artifacts:

`UNVERIFIED`

## Success Criteria

`UNVERIFIED`

## Failure Criteria

`UNVERIFIED`

## Strongest Opposing Explanation

`UNVERIFIED`

## Disproof Plan

`UNVERIFIED`

## Ablation

`UNVERIFIED`

## Rollback

`UNVERIFIED`

## Reviewer Conclusion

`UNVERIFIED`
