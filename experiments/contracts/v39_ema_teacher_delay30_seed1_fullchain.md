# Experiment Contract: v39_ema_teacher_delay30_seed1_fullchain

## Experiment Name

`v39_ema_teacher_delay30_seed1_fullchain`

## Date

2026-07-06

## Baseline Commit

Baseline evidence is the user-provided trusted reproduction directory `/mnt/data/lhp/SDCL2/logs-part1/sysu_train_ori_test2/`; current infrastructure commit before this contract is `5eeff06`.

## Baseline Configuration

SYSU-MM01 full-chain all-search plus indoor 10-trial evaluation, seed recorded by legacy log as the trusted reproduction anchor, cmlabel 30, 60 epochs, source trainer path.

## Problem

v39 collapses during Stage-1 before any Stage-2 contribution can be evaluated.

## Evidence

Log paths and exact evidence:

`logs/0706/log_sysu_v39_ema_teacher_seed1_fullchain.txt:107` enables EMA teacher with `start_epoch=0`; `:111` uses EMA teacher for epoch 0 clustering; `:376-377` reports epoch 1 outliers IR 2456 and RGB 2829. The trusted reproduction log `/mnt/data/lhp/SDCL2/logs-part1/sysu_train_ori_test2/log.txt:327-328` reports epoch 1 outliers IR 1368 and RGB 1693. `logs/0706/sysu_v39_ema_teacher_seed1_fullchain/summary.md` reports best/final epoch 22 all 12.31/13.22/6.29 and indoor 14.08/21.49/18.01.

## Gate Decision

PASS from `sdcl-research-gate`.

## Gate Output Path

`experiments/contracts/v39_ema_teacher_delay30_seed1_fullchain.gate.md`

## Hypothesis

Falsifiable: if the v39 failure is caused by stale EMA teacher features in Stage-1 clustering, then delaying EMA teacher use until `cmlabel=30` should restore the Stage-1 cluster outlier decline and epoch 2-7 mAP trajectory. If those diagnostics do not recover, reject the fix.

## Mechanism

Keep the v39 EMA teacher implementation, but prevent it from generating Stage-1 DBSCAN/CRA pseudo-labels. Stage-1 clustering uses the student model as in the stable path; EMA teacher starts only from the Stage-2 handoff.

## Changed Files

`train_sysu_v39_ema_teacher.sh`

## Independent Variable

One primary variable: default `EMA_TEACHER_START` changes from 0 to 30.

## Fixed Variables

Dataset SYSU-MM01, full-chain all-search plus indoor protocol, seed 1 diagnostic, cmlabel 30, epochs 60, iters 200, batch size 96, num-instances 16, eps 0.6, k1/k2 unchanged, Stage-2 softweight and tailtrim unchanged, checkpoint source generated by this run, evaluation unchanged.

## Intermediate Metric

Cluster/outlier diagnostics should move first: epoch 1-7 IR/RGB outlier counts should drop toward the trusted reproduction and v38 ranges; all-search mAP should recover above 28 by epoch 2 and above 40 by epoch 7.

## Falsification Result

Reject if epoch 2 all-search mAP is below 25, epoch 7 all-search mAP is below 38, or epoch 7 RGB/IR outliers remain above 1500/1000 without traceback, CUDA OOM, NaN, RuntimeError, or loss explosion.

## Main Metrics

- all-search Rank-1/mAP/mINP.
- indoor Rank-1/mAP/mINP.

## Diagnostic Metrics

Cluster counts, IR/RGB outliers, Stage-1 best epoch, loss range from monitor, `clustering_source` lines, checkpoint presence, final parser summary.

## Dataset And Protocol

SYSU-MM01 using the existing repository SYSU all-search and indoor evaluation loops, 10 gallery trials as printed by the current script.

## Seeds

Diagnostic seed: 1.

Full seed list: 1, 2, 3.

Repeated-baseline comparison source: trusted reproduction anchor plus required future repeated baseline runs.

## Baseline Variance

Baseline variance is not yet measured; this run is diagnostic only and cannot claim improvement. A keep decision requires repeated baseline variance from seeds 1, 2, and 3.

## Budget

One full seed-1 diagnostic run, 60 epochs, two RTX 4090 GPUs via `CUDA_VISIBLE_DEVICES=0,1`; early stop if falsification thresholds fail by epoch 7 or if monitor reports loss critical, CUDA OOM, traceback, NaN, or RuntimeError.

## Training Command

`source /home/lhp/miniconda3/etc/profile.d/conda.sh && conda activate sdcl && CUDA_VISIBLE_DEVICES=0,1 SEED=1 scripts/run_experiment_screen.sh v39_ema_teacher_delay30 train_sysu_v39_ema_teacher.sh /home/lhp/project/SDCL2/logs/0706/sysu_v39_ema_teacher_delay30_seed1_fullchain /home/lhp/project/SDCL2/logs/0706/log_sysu_v39_ema_teacher_delay30_seed1_fullchain.txt`

## Monitor Command

`INTERVAL=300 LOSS_WARN_THRESHOLD=70 LOSS_CRITICAL_THRESHOLD=100 scripts/watch_experiment_screen.sh v39_ema_teacher_delay30 train_v39_ema_teacher_delay30 /home/lhp/project/SDCL2/logs/0706/sysu_v39_ema_teacher_delay30_seed1_fullchain /home/lhp/project/SDCL2/logs/0706/log_sysu_v39_ema_teacher_delay30_seed1_fullchain.txt 300`

## Monitor Log Path

`/home/lhp/project/SDCL2/logs/0706/sysu_v39_ema_teacher_delay30_seed1_fullchain/monitor.log`

## Log Directory

`/home/lhp/project/SDCL2/logs/0706/sysu_v39_ema_teacher_delay30_seed1_fullchain`

## Summary Path

`/home/lhp/project/SDCL2/logs/0706/sysu_v39_ema_teacher_delay30_seed1_fullchain/summary.md`

## Parser Command

`python scripts/parse_sysu_log.py /home/lhp/project/SDCL2/logs/0706/log_sysu_v39_ema_teacher_delay30_seed1_fullchain.txt > /home/lhp/project/SDCL2/logs/0706/sysu_v39_ema_teacher_delay30_seed1_fullchain/summary.md`

## Registry Row

Experiment ID `v39_ema_teacher_delay30_seed1_fullchain` is planned in `experiments/registry.csv`.

## Checkpoint Plan

Expected paths: `/home/lhp/project/SDCL2/logs/0706/sysu_v39_ema_teacher_delay30_seed1_fullchain/checkpoint.pth.tar`, `/home/lhp/project/SDCL2/logs/0706/sysu_v39_ema_teacher_delay30_seed1_fullchain/model_best.pth.tar`, `/home/lhp/project/SDCL2/logs/0706/sysu_v39_ema_teacher_delay30_seed1_fullchain/20model_best.pth.tar`. Stage-2 source is this run's `20model_best.pth.tar`; full-state restore is not used.

## Environment

Training environment: `conda activate sdcl`, Python 3.9.18, PyTorch 2.3.0+cu121, CUDA available, 2 GPUs, GPU0 NVIDIA GeForce RTX 4090. Current non-training base Python is 3.13.7 and is not used for training.

## Determinism

Script passes `--seed 1`; repository sets Python, NumPy, and Torch seeds in the training script; DataLoader worker seed policy follows current code; cudnn benchmark remains enabled as logged; deterministic algorithms are not forced.

## Smoke Test

Commands and expected pass artifacts:

`bash -n train_sysu_v39_ema_teacher.sh`; `python -m py_compile sdcl_sysu_v39_ema_teacher.py scripts/parse_sysu_log.py`; `python scripts/research_preflight.py --contract experiments/contracts/v39_ema_teacher_delay30_seed1_fullchain.md --experiment-id v39_ema_teacher_delay30_seed1_fullchain --log-dir /home/lhp/project/SDCL2/logs/0706/sysu_v39_ema_teacher_delay30_seed1_fullchain --train-script train_sysu_v39_ema_teacher.sh`.

## Success Criteria

Diagnostic success requires epoch 2 all-search mAP >= 28, epoch 7 all-search mAP >= 40, epoch 7 outliers near v38/trusted reproduction ranges, monitor clean, and checkpoints generated. Final keep requires multi-seed comparison.

## Failure Criteria

Reject if Stage-1 remains on the v39 collapsed trajectory, if loss exceeds critical threshold, if monitor detects traceback/CUDA OOM/NaN/RuntimeError, or if final metrics remain materially below trusted reproduction.

## Strongest Opposing Explanation

The collapse may be caused by another v39 code path or by environment/config differences rather than EMA teacher start epoch.

## Disproof Plan

If delayed teacher still fails by epoch 7, run a student-only v39 diagnostic with `ENABLE_EMA_TEACHER=0` and compare cluster/outlier trajectories before designing a new mechanism.

## Ablation

Required after diagnostic recovery: `EMA_TEACHER_START=30` versus `ENABLE_EMA_TEACHER=0` versus current failed `EMA_TEACHER_START=0`, all using the same seed and then seeds 1/2/3.

## Rollback

Revert `train_sysu_v39_ema_teacher.sh` default `EMA_TEACHER_START` to 0 or set `EMA_TEACHER_START=0` explicitly in the environment for reproduction of the failed run.

## Reviewer Conclusion

Pending after implementation audit and monitor launch; no improvement claim is allowed from this contract alone.
